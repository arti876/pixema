// import { Link, useMatch } from 'react-router-dom';
// import style from './CustomLink.module.scss';

// interface LinkHambProps {
//   children: string;
//   to: string;
//   className: string;
// }

// export default function LinkHamb({ children, to, className }: LinkHambProps) {
//   const match = useMatch({
//     path: to,
//     end: to.length === 1,
//   });

//   return (
//     <Link to={to} className={`${className} ${match ? style.activeHamb : ''}`}>
//       {children}
//     </Link>
//   );
// }
