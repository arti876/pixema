// import { Link, useMatch } from 'react-router-dom';
// import style from './CustomLink.module.scss';

// interface LinkTabProps {
//   children: string;
//   to: string;
//   className: string;
// }

// export default function LinkTab({ children, to, className }: LinkTabProps) {
//   const match = useMatch({
//     path: to,
//     end: to.length === 1,
//   });

//   return (
//     <Link to={to} className={`${className} ${match ? style.activeTab : ''}`}>
//       {children}
//     </Link>
//   );
// }
